{{ if .Values.local }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ .Values.name }}
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
  host: {{ .Values.route.host }}
  port:
    targetPort: {{ printf "%s-%s-listener" .Values.cluster .Values.name }} # Must match skupper listener service name
  to:
    kind: Service
    name: {{ .Values.name }}
    weight: 100
  wildcardPolicy: None
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
{{ end }}
